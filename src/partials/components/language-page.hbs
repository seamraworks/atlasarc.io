<article class="content-page language-doc">
  <header class="page-hero">
    <p class="eyebrow">{{category}}</p>
    <h1>{{title}}</h1>
    <p class="lede">{{summary}}</p>
  </header>

  <section class="content-section language-body">
{{{md content}}}
  </section>
</article>
